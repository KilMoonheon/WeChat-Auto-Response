# WeChat-Auto-Response
A lightweight Windows auto-reply bot for WeChat PC (4.1.12+). Configure replies per contact via YAML—keyword or fixed messages, with per-chat cooldown. Background message detection; sends via UI automation.
